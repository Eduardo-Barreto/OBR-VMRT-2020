#include <Arduino.h>

int reflecLightFrente = analogRead(A0);
int reflecLight1 = analogRead(A1);
int reflecLight2 = analogRead(A2);
int reflecLight3 = analogRead(A3);
int reflecLight4 = analogRead(A4);
int reflecLight5 = analogRead(A5);
int rightGreen = analogRead(A6);
int leftGreen = analogRead(A7);

int motorAa